"""Prediction logic (placeholder).

Full implementation in US-4.
"""


def predict(model_path: str, features: dict) -> dict:
    """Load a trained model and predict on input features.

    Args:
        model_path: Path to the saved model file.
        features: Dict of feature name -> value.

    Returns:
        Dict with 'prediction' (yes/no) and 'probability' (float).
    """
    raise NotImplementedError("Prediction will be implemented in US-4.")
